{
  "name": "node-red-project",
  "description": "A Node-RED Project",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "uibuilder/*"
  ],
  "scripts": {
    "build:client": "lerna run build",
    "build:client:watch": "lerna run build:watch",
    "dev": "node-red --p 1881 -u ./ --settings"
  },
  "dependencies": {
    "node-red-contrib-uibuilder": "^6.4.1",
    "node-red-node-mysql": "^1.0.3",
    "node-red-node-random": "^0.4.1"
  },
  "devDependencies": {
    "lerna": "^7.0.1"
  }
}
